# RBMD Speed

A minimal Meta Ray-Ban Display web app that shows only the current real GPS speed in km/h.

It uses browser geolocation over HTTPS. When the GPS API provides native speed, the app uses that value. When native speed is unavailable, it estimates speed from consecutive GPS positions.

Published app:

https://danplenicianu-dot.github.io/rbmd-gps-hud/
